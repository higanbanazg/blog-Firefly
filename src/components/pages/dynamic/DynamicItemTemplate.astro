---
import DynamicEntry from "@/components/pages/dynamic/DynamicEntry.astro";
---

{/* 供 DynamicFeed.svelte 克隆的空壳。结构见 DynamicEntry.astro —— 那里同时也负责
    构建期的服务端渲染，两边共用一份标记，不会各自漂移。 */}
<template data-dynamic-item-template>
	<DynamicEntry />
</template>
